continue button field: users.revisit
question: |
  Edit Defendants
subquestion: |
  ${ users.table }

  ${ users.add_action() }
---
table: users.table
rows: users
columns:
  - Name: |
      row_item.name.full() if defined("row_item.name.first") else ""
  - Address: |
      row_item.address
  - Represented by an attorney?: |
      "Maybe" if row_item.attorney.there_are_any is None else yesno(row_item.attorney.there_are_any)  
edit:
  - name.first
  - address.address
confirm: True
---
continue button field: other_parties.revisit
question: |
  Edit Plaintiffs/Landlord
subquestion: |
  ${ other_parties.table }

  ${ other_parties.add_action() }
  
---
table: other_parties.table
rows: other_parties
columns:
  - Name: |
      row_item.name.full() if defined("row_item.name.first") else ""
  - Address: |
      row_item.address.block() if defined("row_item.address.address") else ""
  - Person or Business: |
      "Person" if row_item.person_type == "ALIndividual" else row_item.person_type
  - Represented by an attorney?: |
      "Maybe" if row_item.attorney.there_are_any is None else yesno(row_item.attorney.there_are_any)  
edit:
  - name.first
  - address.address
---
confirm: True
continue button field: other_parties[0].attorney.revisit
question: |
  Attorney for ${ other_parties[0] }
subquestion: |
  ${ other_parties[0].attorney.table }

  ${ other_parties[0].attorney.add_action() }
---
table: users[0].attorney.table
rows: users[0].attorney
columns:
  - Name: |
      row_item.attorney.name.full() if defined("row_item.name.first") else ""
  - Address: |
      row_item.attorney.address.block() if defined("row_item.address.address") else ""
edit:
  - name.first
  - address.address
---
continue button field: users[0].attorney.revisit
question: |
  Attorney for ${ users[0] }
subquestion: |
  ${ users[0].attorney.table }

  ${ users[0].attorney.add_action() }
---
table: users[0].attorney.table
rows: users[0].attorney
columns:
  - Name: |
      row_item.name.full() if defined("row_item.name.first") else ""
  - Address: |
      row_item.address.block() if defined("row_item.address.address") else ""
edit:
  - address.address
  - name.first
---
table: other_parties[0].attorney.table
rows: other_parties[0].attorney
columns:
  - Name: |
      row_item.name.full() if defined("row_item.name.first") else ""
  - Address: |
      row_item.address.block() if defined("row_item.address.address") else ""
edit:
  - address.address
  - name.first
---
continue button field: false_promises.revisit
question: |
  Edit False Promises Made By Landlord
subquestion: |
  ${ false_promises.table }
  
  ${ false_promises.add_action() }
---
table: false_promises.table
rows: false_promises
columns:
  - no label: |
      row_item
edit: True
---
continue button field: offer_attempts.revisit
question: |
  Edit Attempts to Pay Rent
subquestion: |
  ${ offer_attempts.table }
  
  ${ offer_attempts.add_action() }
---
table: offer_attempts.table
rows: offer_attempts
columns:
  - Attempt Date: |
      row_item.attempt_date
  - Attempt Method: |
      row_item.attempt_method
  - Was Attempt to Pay Full Rent?: |
      row_item.was_attempt_full
  - Did The Landlord Refuse This Attempt?: |
      row_item.has_refused_payment
  - Do You Have Evidence of this Attempt?: |
      row_item.has_attempt_evidence if defined('row_item.has_attempt_evidence') else ""
edit:
  - attempt_date
  - attempt_method
  - was_attempt_full
  - has_refused_payment
---
